
# Use a minimal, non-existent base image for the smallest possible size.
# The Wasm binary is self-contained and requires no OS.
FROM scratch

# Copy the compiled Wasm module into the image.
# The `filter.wasm` file is generated by the `tinygo build` command.
COPY filter.wasm /filter.wasm

# No entrypoint is needed as the Wasm binary is not executed directly,
# but loaded by the Envoy proxy.
